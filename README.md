# Contacts App
## A sample contacts app built using React Native

A project built as part of an introductory React Native/Web Service course. The app performs
simple CRUD operations by connecting to a ASP.NET Web Service.
